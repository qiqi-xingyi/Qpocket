# Author: Yuqi Zhang
"""full_pipline examples package.

Made importable so the test suite can run the example runners as modules
(e.g. ``python -m examples.run_smoke``). Scripts inside this
package guard their ``main()`` under ``if __name__ == "__main__"`` so
importing them has no side effects.
"""
